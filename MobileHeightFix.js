const MobileHeightFix = require('./index.js');

/**
 * Initializes the MobileHeightFix class
 * sets the full height variable on the HTML root element
 * and adds a resize event listener
 */
new MobileHeightFix().init();
